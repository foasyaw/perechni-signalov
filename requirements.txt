# Перечни сигналов — зависимости.
# Установка:  pip install -r requirements.txt
#
# Версии зафиксированы по нижней границе: на них проверено.
# ezdxf после 1.4 менял API отрисовки, поэтому нижняя граница важна.

ezdxf>=1.4,<2
python-docx>=1.1
openpyxl>=3.1
matplotlib>=3.8
pillow>=10.0

# Перетаскивание файлов мышью. Без него программа работает, но файлы
# приходится добавлять кнопкой — а сообщение об этом уходит в журнал.
tkinterdnd2>=0.4; python_version >= "3.8"

# pymupdf нужен только для самопроверки PDF (tests), в работе программы не участвует.
